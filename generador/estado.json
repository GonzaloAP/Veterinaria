{
  "fields": [    
    {
            "name": "descripcion",
            "type": "select",
            "options": {
                "Libre": "Libre",
                "Reserva": "Reserva"
            }
    }
  ],
  "validations": [
    {
      "field": "descripcion",
      "rules": "required"
    }
  ]
}